# りすぺく島RPG Ver.1.12

- 「ここから先は、僕ひとりで行く。」の最後のAで止まる問題を修正。
- 封印の洞窟への移動を `enterSealedCave()` に一本化。
- スマホのAボタンは封印イベント中だけ専用入力経路を使用し、通常フィールド／メニュー入力へ流れないよう修正。
- 洞窟開始位置を x=220 に変更し、入った直後の二重入力で出口へ戻る問題を防止。
- 洞窟出口の判定を x<78（移動による自動退出は x<42）まで縮小。
- Ver.1.11で封印イベントの最後に止まった状態からも、そのままAで洞窟へ進めるよう対応。
